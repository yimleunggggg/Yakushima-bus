# 区间班次校验（2026-05-20）

## 问题
`data.js` 环线把多圈时刻混在同一 `trip`。会出现不可能区间、95 分钟「假直达」（东线 11:34→13:09 等端点凑数但沿途不连贯）。

## 修复 `app-core.js?v=core-v6`
- `resolveTripSegment`：沿站序单调行走，**链终点必须是到达站**
- `segmentStops`：起终点之间**全部站点**；无时刻显示 `—`
- `findTrips` / `getStopDepartures` 共用

## 宫之浦港→安房（工作日，对照 PDF）
- **剔除**：11:34→13:09、12:44→14:09 等东线错圈
- **保留**：8:00→8:39、11:15→11:58 等西向列，共约 **8 班**

展开卡显示完整站序；有准确时刻的站显示时间，其余为 `—`（底表 PDF 提取缺中间站时刻）。
