# 常用区间与无车展示（2026-05-20）

## 去重（全局时刻表页）
- **无班次**：只在结果区显示一条说明；顶部「下一班」条隐藏（不再与 empty 重复）
- **今日已结束**：只在顶部条标题显示；去掉结果区 notice 条、末班重复文案
- **班次卡**：去掉 meta 里的起终点重复、倒计时重复；去掉展开后与主卡重复的时间轴（仅保留「继续开往…」时方可展开）

## 常用区间
`presets` 仅展示 `findTrips` 有结果的区间；HTML 内硬编码 chips 已删。

## 大川瀑布
环线脏数据导致校验 0 班，已从 presets 移除；手动选站无结果时提示查官方 PDF。
