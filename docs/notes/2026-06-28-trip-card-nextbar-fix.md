# 班次卡片与下一班条修复（2026-06-28）

## 班次卡片
- 恢复中间列显示行程时长（`→` + `42 分钟`），固定中列宽度对齐各卡时间
- 运营公司 badge 移回 `trip-sub` 与支付标签同一行（路线名下方）
- 右侧 `trip-aside` 仅保留展开箭头
- `segmentStops` 列模式：起终点之间全部站点列出，无时刻显示 `—`

## 下一班条逻辑
- 勾选「只看之后」且无结果：隐藏下一班条，由空状态承接
- 未勾选且列表展示全天已过班次：隐藏「今日已结束」条，避免与列表重复

缓存：`layout-v140`、`core-v13`
