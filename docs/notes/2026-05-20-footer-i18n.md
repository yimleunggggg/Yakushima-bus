# 页脚三语与徒步页底部（2026-05-20）

## 问题
- 中文模式下徒步页底部仍显示日文导航（`site-chrome` 的 `lang` 未与 `AppCore` 同步）
- 页脚链接挤成一行，与免责声明间距过紧

## 修复
- `readLang()` 优先 `AppCore.resolveLang()`
- `renderFooter()` 每次重读语言；`AppCore.notifyLangChange` → `SiteChrome.refresh()`
- 徒步 `render()` 末尾 `SiteChrome?.refresh()`
- 页脚改 3×2 网格（窄屏 2 列），去掉 `·` 分隔
- 三语 `footerNavAria`；`.trek-source` / `.trek-disclaimer` / `.trek-page .app-footer` 间距

## 缓存
- `site-chrome.js` → `chrome-v19`
- `app-core.js` → `core-v7`
- 徒步 `styles.css` → `layout-v121`
