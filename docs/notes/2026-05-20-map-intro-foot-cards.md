# 地图页顶栏与底部卡片统一（2026-05-20）

## 问题
- 顶部说明拆成三行 `app-meta`，与下方 panel 之间多一条分隔线，显得乱。
- 底部温泉卡有图标/箭头，厕所 PDF 卡样式窄且不一致。

## 改动
- 合并为 `.guide-page-intro`（说明 + 链接 · 更新日期一行）。
- 底部统一 `guide-foot-link` 组件（图标 + 标题/说明 + ›）；桌面三列等高。
- 列表列宽 38%、筛选 chip 略放大。`layout-v97` / `guide-v30`。
