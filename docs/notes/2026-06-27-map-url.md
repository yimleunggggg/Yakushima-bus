# 地图页 URL：/guide/ → /map/（2026-06-27）

- **canonical**：`https://yakushimabus.com/map/`（便利设施 POI 地图）
- **`/guide/`**：301 式跳转至 `/map/`（保留 query/hash）
- **`/fare/`**：公交票价（原误用 `/map/` 的旧链仍由根目录 `map.html` 跳转到 `/fare/`）

GSC：sitemap 已更新；对 **`/map/`** 请求编入索引；`/guide/` 可不单独提交。
