# 时刻表 / 徒步页 UI 修复（2026-06-27）

## 改动

1. **页头间距**：去掉 `.page-head-row` 的 `min-height: 2.75rem`，收紧与 cross 链接间距。
2. **无班次提示**：按当前日种标题 + 列出「仅有：周日·节假日 1 班」等；唯一可用日种时给「切换到…」按钮；常用区间按钮 `title` 标注日种。
3. **日祝班次少**：结果区 `results-notice` 说明「日祝仅 2 班（平日 10 班，与官方一致）」——宫之浦港→机场 PDF 日祝确为 2 班。
4. **只看之后**：切换工作日/周六/日祝不再调用 `resetUpcomingFilter()`，用户取消勾选后保持。
5. **下一班占位**：初始 HTML 去掉 `has-next`/`show`，避免加载前空白区。
6. **徒步卡片**：桌面端改 `column-count: 2`，各列独立堆叠，消除右列大空隙。

## 验证

- 宫之浦港入口/宫之浦 → 自然馆 工作日早班 4:46/4:48→约 5:29
- 冬季 12–2 月另显示松叶 4:00 早班
- `python3 scripts/audit_fares.py` → map-data 与 PDF 表一致
